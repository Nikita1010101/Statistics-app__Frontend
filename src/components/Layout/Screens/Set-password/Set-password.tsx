import React, { FC } from 'react'
import styles from './Set-password.module.scss'

export const SetPassword: FC = () => {
	return (
		<>
			<div className={styles.set_password}>Set-password</div>
		</>
	)
}

/* 

  1. 2 inputs for password (enter and accept)

  2. button for submit password

*/
